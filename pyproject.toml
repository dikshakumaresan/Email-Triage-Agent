[project]
name = "email-triage-agent"
version = "0.1.0"
dependencies = [
    "openenv>=0.2.0",
    "openenv-core>=0.2.0",
    "fastapi",
    "uvicorn"
]


[project.scripts]
server = "server.app:app"
dependencies = [
    "openenv>=0.2.0",
    "openenv-core>=0.2.0",
    "fastapi",
    "uvicorn"
]
